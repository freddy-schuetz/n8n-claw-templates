{
  "id": "vexa-meetings",
  "name": "Vexa Meetings",
  "version": "2.3.0",
  "updated": "2026-08-18",
  "type": "native",
  "description": "Deploy transcription bots to Google Meet, Teams, and Zoom meetings, get transcripts, and share them (Vexa.ai API)",
  "category": "meetings",
  "credentials_required": [
    {
      "key": "vexa_api_key",
      "label": "Vexa Bot API Key",
      "hint": "The bot key, starts with vxa_bot_. Drives deploy_meeting_bot, list_bots and stop_bot. Get it at vexa.ai/dashboard under API keys."
    },
    {
      "key": "vexa_tx_key",
      "label": "Vexa Transcription API Key",
      "hint": "The transcription key, starts with vxa_tx_. Drives get_transcript and share_transcript. Vexa rejects the bot key on these endpoints with HTTP 403, so both keys are required."
    }
  ],
  "credentials_optional": [
    {
      "key": "vexa_url",
      "label": "Vexa Base URL (self-hosted)",
      "hint": "Only for self-hosted Vexa instances, e.g. http://localhost:8056. Leave empty to use the Vexa cloud."
    },
    {
      "key": "vexa_bot_name",
      "label": "Bot display name",
      "hint": "Name the bot shows in every meeting, e.g. your assistant’s name. Set it once and it no longer depends on the model passing bot_name. Leave empty for \"Meeting Notetaker\"."
    },
    {
      "key": "vexa_language",
      "label": "Transcription language",
      "hint": "Language code such as de or en. Set this. Without it Vexa detects the language per segment, which turned one German meeting into a transcript in five languages. Overrides what the model sends."
    },
    {
      "key": "vexa_timezone",
      "label": "Time zone for timestamps",
      "hint": "IANA zone such as Europe/Vienna. Vexa timestamps are UTC; without this the minutes show a meeting that ran at 08:37 as 06:37. Empty means UTC, which is then labelled as such."
    }
  ],
  "tools": [
    {
      "name": "deploy_meeting_bot",
      "description": "Deploy a transcription bot and return its Vexa run ID. Refuses a second bot in the same meeting. Language and display name come from the instance settings"
    },
    {
      "name": "get_transcript",
      "description": "Get the transcript of a meeting, grouped into speaker blocks with timestamps. Refuses rather than return a different session when a vexa_run_id is given and does not match"
    },
    {
      "name": "share_transcript",
      "description": "Create a public, time limited link for a meeting transcript"
    },
    {
      "name": "list_bots",
      "description": "List all currently running meeting bots"
    },
    {
      "name": "stop_bot",
      "description": "Stop and remove a bot from a meeting"
    }
  ],
  "notes": [
    "BREAKING in 2.0.0: Vexa split its API into two scopes with separate keys. An installation that only has vexa_api_key will fail on every transcript call with HTTP 403 'Insufficient scope for this endpoint'. Add vexa_tx_key to fix it.",
    "get_transcript returns up to 60000 characters, which is more than the 5000 character guideline for templates. A one hour meeting is roughly 70000 characters of speech, and a truncated transcript is useless for writing minutes. Beyond that limit the tool tells the model to fetch the rest with from_time and to_time.",
    "Passages the transcription service cannot attribute are marked '(unknown speaker)' and are never folded into the previous speaker, so the model cannot invent an attribution.",
    "Vexa addresses transcripts by the meeting room ID, and a recurring meeting reuses that ID for every session. GET /transcripts/{platform}/{id} then returns only the MOST RECENT run, silently, even when that run failed with zero segments while an older run holds the real recording. Verified against the live API on 2026-08-13: no query parameter and no alternative endpoint reaches an older run. deploy_meeting_bot therefore returns a run ID and get_transcript refuses when the run it received is not the one asked for. Older sessions of a recurring meeting are not retrievable at all.",
    "Field tested at a customer on 17. and 18.08.2026, five findings, all fixed in 2.3.0: an empty language from the model made Whisper detect per segment and 21% of a German meeting came back in English, Russian, Portuguese, French, Polish, Danish and Spanish, so instance settings now win over the model. Several people sharing one device in a call are reported by Vexa as a single speaker, and the old 12 second grouping gap merged 394 segments into ONE paragraph of 24000 characters, so the gap is 2 seconds and a note explains why names cannot be assigned. Timestamps were served raw in UTC and the minutes were two hours off. list_bots does not show a bot that is still joining, which produced two bots in one meeting, so deploy now checks /meetings first. And a 404 from stop_bot was being read as \"the bot already left\" while it was still in the room."
  ],
  "author": "freddy-schuetz",
  "license": "MIT",
  "tested_n8n_version": "2.21.4"
}
